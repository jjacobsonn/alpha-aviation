# API service (monorepo root). Forces Docker build so Railpack does not detect root package.json.
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.railway-api"

# No healthcheckPath — migrate + gunicorn startup can exceed default probe window on Hobby.
