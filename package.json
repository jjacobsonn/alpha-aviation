{
  "name": "alpha-aviation",
  "private": true,
  "description": "Alpha Aviation monorepo — backend and frontend",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && poetry run python manage.py runserver",
    "dev:frontend": "cd frontend && npx react-scripts start",
    "test:e2e": "playwright test --config=e2e/playwright.config.ts",
    "test:e2e:headed": "playwright test --config=e2e/playwright.config.ts --headed",
    "install:all": "npm run install:backend && npm run install:frontend",
    "install:backend": "cd backend && poetry install",
    "install:frontend": "cd frontend && yarn install"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.0",
    "concurrently": "^9.1.0"
  }
}
