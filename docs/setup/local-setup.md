# Local Setup (Deprecated)

This file is kept for compatibility with older references.

Use [DEVELOPMENT.md](DEVELOPMENT.md) as the canonical setup guide. It includes:

- complete database setup
- backend and frontend install/run commands
- full-stack verification/testing steps
- troubleshooting for common local issues

Quick start from repo root:

```bash
npm run dev
```
